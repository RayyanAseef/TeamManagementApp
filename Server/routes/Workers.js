const express = require('express');
const router = express.Router();

const { Workers } = require('../models');

// Get all of that table

// Get one of that table

// Delete one of that table

// Edit one of that table

module.exports = router